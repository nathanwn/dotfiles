# ==============================================================================
#                                 System bar
# ==============================================================================
# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
# # NetworkManager applet
# exec --no-startup-id nm-applet
# # Blueman applet
# exec --no-startup-id blueman-applet
# # Volume
# exec --no-startup-id pasystray
# # Power manager
# exec --no-startup-id xfce4-power-manager
# # Ibus
# exec --no-startup-id ibus-daemon -x -d -r

# ==============================================================================
#                                   Settings
# ==============================================================================
# Clipboard for xfce4-screenshooter
# exec --no-startup-id xfce4-clipman
# External monitors
# > work
# exec --no-startup-id xrandr --output $monitor_lap --primary --auto --pos 3002x640 --output $monitor_h --auto --pos 1081x0 --output $monitor_v --auto --pos 0x-480 --rotate right
# > home
# exec --no-startup-id xrandr --output $monitor_h --primary --auto --pos 0x0 --output $monitor_lap --auto --pos 1920x600

# ==============================================================================
#                                     Apps
# ==============================================================================
# exec --no-startup-id slack
# exec --no-startup-id google-chrome-stable
# exec --no-startup-id chromium

# ==============================================================================
#                               Function keys
# ==============================================================================
# Use pactl to adjust volume in PulseAudio.
# set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $refresh_i3status
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $refresh_i3status
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
#
# # Screen brightness - controlled by `light`
# bindsym XF86MonBrightnessUp     exec light -A 2 # increase screen brightness
# bindsym XF86MonBrightnessDown   exec light -U 2 # decrease screen brightness
