# - name: pip - Install pip
#   become: true
#   apt:
#     name:
#       - python3-pip
#   tags:
#     - pip
#
# - name: pip - Install tldr
#   pip:
#     name: tldr
#   tags:
#     - pip
#
# - name: pip - Install shtab
#   pip:
#     name: tldr
#   tags:
#     - shtab
#
# - name: pip - Install virtualenv for neovim
#   pip:
#     requirements: "{{ lookup('env', 'HOME') }}/.local/share/nvim/requirements.txt"
#     virtualenv_command: "python3 -m venv"
#     virtualenv: "{{ lookup('env', 'HOME') }}/.local/share/nvim/venv"
#   tags:
#     - pip
